$("#detailModal").modal("hide");

toastr.success("<%= j notice %>");

$("#indicator_details").html(
  '<%= j render(partial: "detail",
    collection: @indicator.indicator_detail_rows,
    locals: {indicator: @indicator}) %>'
);
